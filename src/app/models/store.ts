export interface Store {
    id: number; // Unique identifier for the store
    name: string; // Name of the store
    address: string; // Address of the store
    imageurl: string; // URL of the store's image
  }